val x  = new SimplePodcastTools
//val extensions = Array("mp4", "m4a")
//val path = "/Users/Nenyi/Music/podcasts/"
//val dest = "/Users/Nenyi/Downloads/podcast"

//x processBBCPodcasts

//val podcasts = x getBBCPodcastFiles(path, extensions)
//podcasts.length
//val t = podcasts.map(p =>
//   x getPodcastDestination(
//   x extractAndRenamePodcastTags(
//   x getPodcastTags(p), p), dest))
//  .map(p => x writeToPodcast(p))
//  .map(p => x renameFile(p))
////println(t)
////t.head.destDir
////t.head.fileName
// t.map(p => x movePodcast(x mapPodcastDestination(p)))
////t.head.destDir
//
////
////println(podcasts)
//
//val podcasts2 = x getPodcastFiles(path, extensions)
//val t2 = podcasts2.map(p =>
// x getPodcastDestination(
// x extractPodcastTags(
// x getPodcastTags(p), p), dest))
//t2.length
//
// t2.map(p => x movePodcast(x mapPodcastDestination(p)))
////
////
////z1.map(s => println(s.destDir))